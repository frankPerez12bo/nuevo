// Ingresar la cadena
let cadena = prompt("Ingrese una cadena:");
// Verificar si la cadena tiene 5 caracteres o más usando el operador ternario
let resultado = (cadena.length <= 5) ? true : false;
// Mostrar el resultado
document.write(resultado);